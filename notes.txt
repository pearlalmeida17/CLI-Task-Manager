def load_tasks():
    open Json file tasks.json:
        if it doesn't exist create on with empty list []

        if it exists read the contents of it
            convert it's contents to a dictionary using to_dict() func
                and print it in an organized manner 




def save_tasks(tasks: list[tasks ]):
    define file path
    if the file doesn't exixt :
        create it
    if the file exists :
        the function takes a parameter of tasks_list
        read each element of the list using for loop
             convert the task objects into dictionary using to dictionary method in tasks
             input it the json file using .write_text()


